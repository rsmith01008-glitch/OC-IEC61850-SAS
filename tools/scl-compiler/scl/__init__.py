"""SCL (IEC 61850-6) -> sas-ied.cfg/sas-scada.cfg compiler.

Offline, build-time only -- never runs on OC hardware. See
tools/scl-compiler/README.md for the compiler's invocation and
tools/scl-compiler/schema/README.md for the vendored XSD's provenance.
"""
