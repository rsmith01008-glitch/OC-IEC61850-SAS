Placeholder: per MineOS's own documentation (System-API.md, "How to
develop MineOS applications?"), every ".app" bundle's icon is a file named
Icon.pic sitting directly in the application directory root -- not in a
"Resources/" subfolder, and not referenced from any manifest table (there
is no MineOS-read manifest file at all -- confirmed by grepping the whole
wiki for "About"/"manifest": nothing). Create Icon.pic here (alongside
Main.lua) with MineOS's own icon/image tooling once you have a real
MineOS install to work from -- the wiki documents that ".pic" images are
loaded via image.load() but doesn't specify exact pixel dimensions/format,
so that detail is still unverified. Until Icon.pic exists, MineOS should
fall back to a default icon for this application.
